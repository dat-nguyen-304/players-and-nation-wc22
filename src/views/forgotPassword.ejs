<!DOCTYPE html>
<html lang="en">

<%- include('./common/head.ejs') %>

    <body>
        <div id="main">
            <div class="landing">
                <div class="dark-overlay">
                    <div class="landing-inner">
                        <form class='my-4' onSubmit="" action="/api/forgot-password" method="POST">
                            <h1>World cup 2022</h1>
                            <h4>Keep memories of this year's world cup season</h4>
                            <% if (message) { %>
                                <div class="alert alert-danger" role="alert">
                                    <%= message %>
                                </div>
                                <% } %>
                                    <div class='my-4'>
                                        <input type='email' class="form-control" placeholder='Enter your email'
                                            name='username' onChange="" value="<%= username %>" />
                                    </div>
                                    <button class="Btn Btn--success" variant='success' type='submit'>Enter</button>
                        </form>
                        <p style="display: flex; align-items: center;">
                            Back to
                            <a href='/login-page' style="margin-left: 1rem; text-decoration: none;">
                                <button class="Btn Btn--primary login-register-btn" variant='info' size='sm'
                                    class='ms-2'>Login</button>
                            </a>
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </body>

</html>